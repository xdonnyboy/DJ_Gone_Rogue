using UnityEngine;
using System.Collections;

public class DJ_ManagerScript : MonoBehaviour
{
	public DJ_ManagerScript()
	{

	}

	// Use this for initialization
	public virtual void Start ()
	{
	
	}
	
	// Update is called once per frame
	public virtual void Update ()
	{
	
	}

	public virtual void FromFile(string path)
	{

	}

	/// <summary>
	/// Releases all resource used by the <see cref="DJ_ManagerScript"/> object.
	/// </summary>
	/// <remarks>Call <see cref="Dispose"/> when you are finished using the <see cref="DJ_ManagerScript"/>. The <see cref="Dispose"/>
	/// method leaves the <see cref="DJ_ManagerScript"/> in an unusable state. After calling <see cref="Dispose"/>, you must
	/// release all references to the <see cref="DJ_ManagerScript"/> so the garbage collector can reclaim the memory that
	/// the <see cref="DJ_ManagerScript"/> was occupying.</remarks>
	public virtual void Dispose()
	{
		
	}
}
